[tool.ruff]
target-version = "py310"
line-length = 100
exclude = [
  "ats_backend",
  "uploads"
]

[tool.ruff.lint]
select = ["E", "F"]

[tool.ruff.lint.per-file-ignores]
"migrations/env.py" = ["E402"]